sudo: required
addons:
  apt:
    packages:
      - docker-ce

services:
 - docker

language: python
python:
  - "3.5"

before-install:
  - pwd
  - ls -la
  - docker-compose up -d
  - docker --version
  - pip install selenium

script:
  - echo "Init containers..."
  - sleep 15
  - echo "Stating tests..."
  - python test/*.py
